# charsyam/homebrew-tap

Homebrew tap for charsyam's tools.

## Usage

```sh
brew tap charsyam/tap
brew install charsyam/tap/soltop
```

## Formulae

- **soltop** — Apple Silicon GPU/CPU/power monitor (no sudo, no powermetrics).
  See https://github.com/charsyam/soltop
